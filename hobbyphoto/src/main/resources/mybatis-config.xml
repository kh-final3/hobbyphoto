<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "mybatis-3-config.dtd" >
<configuration>
	
	<settings>
		<setting name="jdbcTypeForNull" value="NULL"/>
	</settings>
	
	<!--  
	<typeAliases>
		<typeAlias type="" alias=""/>
	</typeAliases>
	-->
	<!-- 미리 등록 하고 싶으면 해당 클래스가 존재하고 있어야 한다. -->
	<!-- type은 클래스 풀네임 alias는 별칭 -->

	<!--  
	<mappers>
		<mapper resource=""/>
	</mappers>	
	-->
	<!-- 미리 등록 하고 싶으면 해당 mapper 파일이 있어야 하고 mapper의 namespace까지 지정되어 있어야한다. -->
	

</configuration>
